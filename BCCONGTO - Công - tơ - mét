#include <bits/stdc++.h>
using namespace std;
int main(){
	while(1){
		string a;
		cin>>a;
		if(a=="0") break;
		int s=0;
		for(int i=0;i<a.size();i++){
			int x=a[i]-'0';
			if(x>4) x--;
			s=s*9+x;
		}
		cout<<a<<": "<<s<<"\n";
	}
}
