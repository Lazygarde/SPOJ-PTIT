#include <bits/stdc++.h>
using namespace std;
int main(){
    long long a,b,v;
    cin>>a>>b>>v;
    if(a==v) cout<<"1";
    else{
        if((v-b)%(a-b)==0) cout<<(v-b)/(a-b);
        else cout<<(v-b)/(a-b)+1;
    }
}
