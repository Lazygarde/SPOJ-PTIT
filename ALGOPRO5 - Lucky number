#include <bits/stdc++.h>
using namespace std;
int main(){
	ios_base::sync_with_stdio(0);
	cin.tie(0);
 	int n;
	cin>>n;
	long long s=0;
	for(int i=1;i<=n;i++){
		long long m=1;
		for(int j=1;j<=i;j++){
			m*=2;
		}
		s+=m;
	}
	cout<<s;
}
