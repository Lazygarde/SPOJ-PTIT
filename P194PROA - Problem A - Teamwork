#include <stdio.h>
int main()
{
	int n, x, ss = 0;
	scanf("%d", &n);
	for (int i = 0; i < n; i++)
	{
		int s = 0;
		for (int j = 0; j < 3; j++)
		{
			scanf("%d", &x);
			if (x == 1)
				s++;
		}
		if (s > 1)
			ss++;
	}
	printf("%d", ss);
}
